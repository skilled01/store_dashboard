// import { useI18n } from "vue-i18n";
//
// const { t, te } = useI18n();
export const translate = (text: string) => {
  // if (te(text)) {
  //   return t(text);
  // } else {
  //   return text;
  // }

  return text;
};

export default { translate };
